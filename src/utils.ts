/* ENUM for robot execution outcome*/
export enum Messages {
  CANNOT_MOVE_OUTSIDE = "Robot cannot move outside the warehouse",
  INVALID_COMMAND = "Invalid command",
  SUCCESS_EXECUTION = "Command executed successfully",
}
